<?xml version="1.0" encoding="utf-8"?>
<!--
  Touch feedback for a Surah Index row: a #D0ECE4 highlight ripple clipped to the row's rounded
  bounds. The row itself stays paper-coloured so the list reads as a flat index, not a stack of cards.
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/quran_highlight">
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="@android:color/white" />
            <corners android:radius="14dp" />
        </shape>
    </item>
</ripple>
