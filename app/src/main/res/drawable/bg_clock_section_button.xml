<?xml version="1.0" encoding="utf-8"?>
<!-- Row background of the clock section chooser. Deliberately the app's own accent, so nothing in the
     Islamic screens borrows a resource (or a colour) from another feature's theme. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/appColor" />
    <corners android:radius="14dp" />
</shape>
