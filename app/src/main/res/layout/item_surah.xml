<?xml version="1.0" encoding="utf-8"?>
<!--
  One row of the Surah Index RecyclerView.

  Physical layout is LTR-anchored on purpose (android:layoutDirection="ltr" on the row): the number
  badge always sits on the left and the Arabic name always on the right, whichever language the
  device is set to. The Arabic name itself is explicitly RTL.

  The whole row is the click target (clickable + focusable) so it is reachable by TalkBack and by
  d-pad/keyboard, not only by touch.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/surah_row"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_surah_row"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center_vertical"
    android:layoutDirection="ltr"
    android:minHeight="84dp"
    android:orientation="horizontal"
    android:paddingStart="16dp"
    android:paddingTop="12dp"
    android:paddingEnd="16dp"
    android:paddingBottom="12dp"
    tools:background="@color/quran_background">

    <!-- Surah number, 1-114, western digits so the index can be scanned quickly. -->
    <TextView
        android:id="@+id/surah_number"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:background="@drawable/bg_surah_badge"
        android:gravity="center"
        android:textColor="@color/quran_header"
        android:textSize="15sp"
        android:textStyle="bold"
        tools:text="2" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/surah_english_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/quran_text"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="Al-Baqarah" />

        <TextView
            android:id="@+id/surah_english_meaning"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/quran_header"
            android:textSize="12sp"
            tools:text="The Cow" />

        <TextView
            android:id="@+id/surah_meta"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/quran_header"
            android:textSize="12sp"
            tools:text="286 Ayahs &#183; Madinah &#183; Juz 1" />
    </LinearLayout>

    <!-- Arabic name, hugging the right edge the way a mushaf index reads. -->
    <TextView
        android:id="@+id/surah_arabic_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="12dp"
        android:gravity="end"
        android:maxWidth="150dp"
        android:textColor="@color/quran_text"
        android:textDirection="rtl"
        android:textSize="22sp"
        tools:text="البقرة" />
</LinearLayout>
