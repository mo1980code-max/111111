<?xml version="1.0" encoding="utf-8"?>
<!--
  Shared shell for the clock and wallpaper grids.

  There is no bottom banner container any more: native ads live *inside* the list (see
  AdInsertingAdapter), which keeps them away from the header and gives a failed ad request nothing to
  intrude on. The hint line states the free/unlock rule before the user taps anything.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/textColor"
    android:orientation="vertical">

        <!-- The banner MUST have a bounded height. With wrap_content, the splash_bg ImageView below
             (match_parent x match_parent) measures against the full remaining screen and stretches
             this header to full screen height, leaving the RecyclerView (weight=1) with 0dp and
             hiding every item. 84dp keeps it compact: 44dp back button + title + hint line. -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="84dp"
            android:orientation="vertical">

            <ImageView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:contentDescription="@null"
                android:scaleType="centerCrop"
                android:src="@drawable/splash_bg" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:gravity="center_vertical"
                android:orientation="vertical"
                android:paddingLeft="6dp"
                android:paddingTop="8dp"
                android:paddingRight="14dp"
                android:paddingBottom="8dp">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center_vertical"
                    android:orientation="horizontal">

                    <ImageView
                        android:id="@+id/ivBack"
                        android:layout_width="44dp"
                        android:layout_height="44dp"
                        android:background="?attr/selectableItemBackgroundBorderless"
                        android:contentDescription="@null"
                        android:padding="10dp"
                        android:src="@drawable/ic_back" />

                    <TextView
                        android:id="@+id/txtTitle"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:ellipsize="end"
                        android:fontFamily="@font/myfont"
                        android:maxLines="1"
                        android:text="@string/wallpaper_title"
                        android:textColor="@color/textColor"
                        android:textSize="21sp" />
                </LinearLayout>

                <TextView
                    android:id="@+id/txtHint"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginLeft="46dp"
                    android:layout_marginTop="1dp"
                    android:textColor="@color/section_hint_on_dark"
                    android:textSize="11sp" />
            </LinearLayout>
        </FrameLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCategory"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingBottom="10dp"
        android:scrollbars="vertical" />
</LinearLayout>
